提供一个相对来说更不容易写错的板子
```cpp
//外层循环扩展右边界，内层循环扩展左边界
for (int l = 0, r = 0 ; r < n ; r++) {
	while (l <= r && check()) {//区间[left,right]不符合题意
        //扩展左边界
    }
    
    //将r加入闭区间，统计和维护相关信息
    
    //区间[left,right]符合题意，统计对答案的贡献
}
```
